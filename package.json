{
  "name": "utils-mongodb",
  "version": "0.0.3",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncu -u && npm install && npm update && tsc",
    "commit": "git commit -am 'commit' && npm version patch && git push"
  },
  "author": "sawara7",
  "license": "MIT",
  "bundleDependencies": [],
  "devDependencies": {
    "typescript": "^5.5.4",
    "@types/node": "^22.5.0"
  },
  "dependencies": {
    "mongodb": "^6.8.0"
  }
}
