{
  "name": "utils-mongodb",
  "version": "0.0.2",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncu -u && npm install && npm update && tsc",
    "commit": "git commit -am 'commit' && npm version patch && git push"
  },
  "author": "sawara7",
  "license": "MIT",
  "bundleDependencies": [],
  "devDependencies": {
    "typescript": "^5.5.3",
    "@types/node": "^20.14.11"
  },
  "dependencies": {
    "mongodb": "^6.8.0"
  }
}
